- waitFor
  - resolve promise once thunk stop throwing error
- define all packages on README
  - each package needs to have installation step
  - example
- publish first package through github actions
